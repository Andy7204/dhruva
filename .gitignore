# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Packaging output and local secrets
dist/
.env

# OS
.DS_Store

# Note: runs/ , reports/dashboard.html and data/cache/ are intentionally NOT
# ignored — committing them makes the first Streamlit/Actions run fast and the
# dashboard render immediately. Remove data/cache/ here if you'd rather keep the
# repo small (the daily cron will refetch prices on its first run).
